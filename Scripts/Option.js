function show(aval) {
    if (aval == "Hot Chocolate" || aval == "Hot Caramel"  || aval == "Hot Vanilla" || aval == "Iced Caramel" || aval == "Iced Vanilla"  ) {
    hiddenDiv.style.display='block';
     hiddenDiv.style.float='left';
    Form.fileURL.focus();
    } 
    else{
    hiddenDiv.style.display='none';
    }
  }
